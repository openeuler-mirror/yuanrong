[pytest]
markers =
    smoke: quick and must-have
    slow: marks tests as slow (deselect with '-m "not slow"')
    serial
    off_cluster: tests that run against a remote cluster from outside (requires YR_SERVER_ADDRESS)