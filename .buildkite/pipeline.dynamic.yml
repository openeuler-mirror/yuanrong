#!/bin/bash
# Buildkite 动态 Pipeline - 标准 SSH 版
set -e
BUILDER_IMAGE="swr.cn-southwest-2.myhuaweicloud.com/yuanrong-dev/compile-ubuntu2004:v20260428_cmake33110"
SANDBOX_PACKAGER_IMAGE="${SANDBOX_PACKAGER_IMAGE:-swr.cn-southwest-2.myhuaweicloud.com/yuanrong-dev/sandbox-packager:v20260506_kubectl}"
SANDBOX_DEPLOYER_IMAGE="${SANDBOX_DEPLOYER_IMAGE:-swr.cn-southwest-2.myhuaweicloud.com/yuanrong-dev/sandbox-deployer:v20260506_kubectl_py39}"
ENABLE_MACOS_SDK="${ENABLE_MACOS_SDK:-true}"
ENABLE_LINUX_ARM="${ENABLE_LINUX_ARM:-true}"
ENABLE_SANDBOX_PACKAGE="${ENABLE_SANDBOX_PACKAGE:-true}"
ENABLE_SANDBOX_K8S_TEST="${ENABLE_SANDBOX_K8S_TEST:-${ENABLE_SANDBOX_BEIJING4_DEPLOY:-true}}"
ENABLE_SANDBOX_MANIFEST="${ENABLE_SANDBOX_MANIFEST:-${ENABLE_LINUX_ARM}}"
SDK_PYTHON_VERSIONS="${SDK_PYTHON_VERSIONS:-python3.9 python3.10 python3.11 python3.12}"
SANDBOX_RUNTIME_IMAGE_PYTHON_VERSIONS="${SANDBOX_RUNTIME_IMAGE_PYTHON_VERSIONS:-${SDK_PYTHON_VERSIONS}}"

is_enabled() {
  case "$1" in
    1|true|TRUE|yes|YES|on|ON) return 0 ;;
    *) return 1 ;;
  esac
}

sdk_python_suffix() {
  case "$1" in
    python3.9) printf 'cp39' ;;
    python3.10) printf 'cp310' ;;
    python3.11) printf 'cp311' ;;
    python3.12) printf 'cp312' ;;
    *)
      printf 'Unsupported SDK Python version: %s\n' "$1" >&2
      exit 1
      ;;
  esac
}

SANDBOX_AMD64_SDK_STEP="build-sdk-amd64-cp39"
SANDBOX_ARM64_SDK_STEP="build-sdk-arm64-cp39"
SANDBOX_MANIFEST_SDK_DEPENDS=""
SANDBOX_MANIFEST_RUNTIME_DEPENDS=""
SANDBOX_RUNTIME_IMAGE_STEPS=""
for SDK_PYTHON_VERSION in ${SDK_PYTHON_VERSIONS}; do
  SDK_SUFFIX="$(sdk_python_suffix "${SDK_PYTHON_VERSION}")"
  SANDBOX_MANIFEST_SDK_DEPENDS="${SANDBOX_MANIFEST_SDK_DEPENDS}"$'\n'"      - \"build-sdk-amd64-${SDK_SUFFIX}\""
  if is_enabled "${ENABLE_LINUX_ARM}"; then
    SANDBOX_MANIFEST_SDK_DEPENDS="${SANDBOX_MANIFEST_SDK_DEPENDS}"$'\n'"      - \"build-sdk-arm64-${SDK_SUFFIX}\""
  fi
  if is_enabled "${ENABLE_MACOS_SDK}"; then
    SANDBOX_MANIFEST_SDK_DEPENDS="${SANDBOX_MANIFEST_SDK_DEPENDS}"$'\n'"      - \"build-sdk-macos-arm64-${SDK_SUFFIX}\""
  fi
done
for SDK_PYTHON_VERSION in ${SANDBOX_RUNTIME_IMAGE_PYTHON_VERSIONS}; do
  SDK_SUFFIX="$(sdk_python_suffix "${SDK_PYTHON_VERSION}")"
  SANDBOX_RUNTIME_IMAGE_STEPS="${SANDBOX_RUNTIME_IMAGE_STEPS:+${SANDBOX_RUNTIME_IMAGE_STEPS} }publish-runtime-amd64-${SDK_SUFFIX}"
  SANDBOX_MANIFEST_RUNTIME_DEPENDS="${SANDBOX_MANIFEST_RUNTIME_DEPENDS}"$'\n'"      - \"publish-runtime-amd64-${SDK_SUFFIX}\""
  if is_enabled "${ENABLE_LINUX_ARM}"; then
    SANDBOX_RUNTIME_IMAGE_STEPS="${SANDBOX_RUNTIME_IMAGE_STEPS} publish-runtime-arm64-${SDK_SUFFIX}"
    SANDBOX_MANIFEST_RUNTIME_DEPENDS="${SANDBOX_MANIFEST_RUNTIME_DEPENDS}"$'\n'"      - \"publish-runtime-arm64-${SDK_SUFFIX}\""
  fi
done

SANDBOX_FINAL_PACKAGE_STEP="publish-sandbox-release-amd64"
if is_enabled "${ENABLE_SANDBOX_PACKAGE}" && is_enabled "${ENABLE_LINUX_ARM}" && is_enabled "${ENABLE_SANDBOX_MANIFEST}"; then
  SANDBOX_FINAL_PACKAGE_STEP="publish-sandbox-manifest"
fi

emit_sandbox_k8s_test() {
cat << YAML
  - label: ":rocket: Test K8S"
    key: "test-k8s"
    depends_on: "${SANDBOX_FINAL_PACKAGE_STEP}"
    command: |
      set -e
      buildkite-agent artifact download "artifacts/ci-source/yuanrong-ci-source.tgz" . --step "build-all-amd64"
      tar -xzf artifacts/ci-source/yuanrong-ci-source.tgz
      export PATH="/workspace:\$\$PATH"
      bash .buildkite/test_sandbox_k8s.sh
    env:
      BUILDKITE_AGENT_NAME: "agent-stack-k8s"
      BUILDKITE_SKIP_CHECKOUT: "true"
      SANDBOX_PACKAGE_STEP_KEY: "${SANDBOX_FINAL_PACKAGE_STEP}"
      SANDBOX_SDK_STEP_KEY: "${SANDBOX_AMD64_SDK_STEP}"
    agents:
      queue: "default"
      os: "linux"
      arch: "amd64"
    plugins:
      - kubernetes:
          podSpec:
            imagePullSecrets:
              - name: swr-pull-secret
            containers:
              - image: "$SANDBOX_DEPLOYER_IMAGE"
                resources:
                  requests: { cpu: "2", memory: "4Gi" }
                  limits: { cpu: "4", memory: "8Gi" }
                env:
                  - name: SWR_USERNAME
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: username
                        optional: true
                  - name: SWR_PASSWORD
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: password
                        optional: true
                  - name: SWR_DOCKER_CONFIG_JSON
                    valueFrom:
                      secretKeyRef:
                        name: swr-pull-secret
                        key: .dockerconfigjson
                        optional: true
    timeout_in_minutes: 60
    artifact_paths:
      - "artifacts/sandbox-smoke/**/*"
YAML
}

emit_sandbox_runtime_image() {
  local image_arch="$1"
  local agent_arch="$2"
  local sdk_suffix="$3"
  local sdk_step="$4"
  local pod_image="$5"
  local buildkit_mode="$6"
  local key="publish-runtime-${image_arch}-${sdk_suffix}"
  local node_selector=""
  local agent_arch_attr="arch"

  if [ "${agent_arch}" = "arm64" ]; then
    agent_arch_attr="linux_arch"
    node_selector="            nodeSelector:
              kubernetes.io/arch: arm64"
  fi

cat << YAML
  - label: ":package: Build Runtime ${image_arch} ${sdk_suffix}"
    key: "${key}"
    depends_on:
      - "build-all-amd64"
      - "${sdk_step}"
    command: |
      set -e
      buildkite-agent artifact download "artifacts/ci-source/yuanrong-ci-source.tgz" . --step "build-all-amd64"
      tar -xzf artifacts/ci-source/yuanrong-ci-source.tgz
      export PATH="/workspace:\$\$PATH"
      bash .buildkite/package_sandbox_release.sh
    env:
      BUILDKITE_AGENT_NAME: "agent-stack-k8s"
      BUILDKITE_SKIP_CHECKOUT: "true"
      SANDBOX_SDK_STEP_KEY: "${sdk_step}"
      YR_K8S_RUNTIME_ONLY: "1"
      YR_K8S_IMAGE_ARCH: "${image_arch}"
      YR_K8S_IMAGE_PLATFORM: "linux/${image_arch}"
      YR_K8S_IMAGE_TAG_SUFFIX: "-${image_arch}-${sdk_suffix}"
      YR_K8S_IMAGE_SDK_WHEEL_PATTERN: "openyuanrong_sdk*-${sdk_suffix}-*.whl"
      YR_K8S_IMAGE_CACHE: "1"
      YR_K8S_IMAGE_CACHE_TAG: "build-cache-runtime-${image_arch}-${sdk_suffix}"
      YR_K8S_DOCKER_BUILDKIT: "${buildkit_mode}"
    agents:
      queue: "default"
      os: "linux"
      ${agent_arch_attr}: "${agent_arch}"
    plugins:
      - kubernetes:
          podSpec:
${node_selector}
            imagePullSecrets:
              - name: swr-pull-secret
            containers:
              - image: "${pod_image}"
                securityContext:
                  privileged: true
                volumeMounts:
                  - name: docker-graph
                    mountPath: /var/lib/docker
                resources:
                  requests: { cpu: "4", memory: "8Gi" }
                  limits: { cpu: "8", memory: "16Gi" }
                env:
                  - name: SWR_USERNAME
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: username
                        optional: true
                  - name: SWR_PASSWORD
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: password
                        optional: true
                  - name: SWR_DOCKER_CONFIG_JSON
                    valueFrom:
                      secretKeyRef:
                        name: swr-pull-secret
                        key: .dockerconfigjson
                        optional: true
            volumes:
              - name: docker-graph
                emptyDir: {}
    timeout_in_minutes: 90
    artifact_paths:
      - "artifacts/sandbox/**/*"
YAML
}

echo "steps:"
cat << YAML
  - label: ":rocket: Build X86"
    key: "build-all-amd64"
    command: |
      set -e
      echo "=== [1/3] Environment Setup ==="
      
      export PATH=/opt/buildtools/python3.9/bin:/usr/local/bin:/usr/bin:\$\$PATH
      export CC=gcc-10
      export CXX=g++-10
      export PIP_BREAK_SYSTEM_PACKAGES=1
      export SDK_PYTHON_VERSIONS="${SDK_PYTHON_VERSIONS}"
      JOBS="\$\${JOBS:-16}"
      FUNCTIONSYSTEM_JOBS="\$\${FUNCTIONSYSTEM_JOBS:-8}"
      export FUNCTIONSYSTEM_JOBS
      COMMIT_FOR_VERSION="\$\${BUILDKITE_COMMIT:-\$\$(git rev-parse HEAD)}"
      SHORT_COMMIT=\$\$(printf '%s' "\$\$COMMIT_FOR_VERSION" | cut -c1-12)
      YR_BUILD_VERSION="\$\${YR_BUILD_VERSION:-\$\${BUILD_VERSION:-0.7.0+\$\$SHORT_COMMIT}}"
      export COMMIT_ID="\$\$COMMIT_FOR_VERSION"
      echo "Package version: \$\$YR_BUILD_VERSION"
      
      CACHE_BASE="/mnt/paas/build-cache"
      mkdir -p \$\$CACHE_BASE/ccache \$\$CACHE_BASE/go-mod \$\$CACHE_BASE/go-cache \
        \$\$CACHE_BASE/opensource \$\$CACHE_BASE/pip \$\$CACHE_BASE/functionsystem-vendor-cache
      export CCACHE_DIR=\$\$CACHE_BASE/ccache
      export CCACHE_MAXSIZE="\$\${CCACHE_MAXSIZE:-100G}"
      ccache -M "\$\$CCACHE_MAXSIZE"
      export GOMODCACHE=\$\$CACHE_BASE/go-mod
      export PIP_CACHE_DIR=\$\$CACHE_BASE/pip
      # Keep Go's content-addressed build cache warm across Buildkite jobs; cache-aging handles old entries.
      export GOCACHE=\$\$CACHE_BASE/go-cache/\$\${BUILD_ARCH:-amd64}
      mkdir -p "\$\$GOCACHE"
      export DS_OPENSOURCE_DIR=\$\$CACHE_BASE/opensource
      export FS_VENDOR_CACHE_DIR=\$\$CACHE_BASE/functionsystem-vendor-cache
      collect_failure_logs() {
        _STATUS="\$\$1"
        _ARCH="\$\${BUILD_ARCH:-x86_64}"
        _LOG_DIR="artifacts/failure-logs/\$\$_ARCH"
        echo "=== Build failed (exit \$\$_STATUS); collecting diagnostic logs ==="
        mkdir -p "\$\$_LOG_DIR"
        [ -f /tmp/build_output.log ] && cp /tmp/build_output.log "\$\$_LOG_DIR/build_output.log"
        for _DIR in \
            functionsystem/vendor/output/Stamp \
            functionsystem/vendor/src \
            functionsystem/build \
            datasystem/build; do
          if [ -d "\$\$_DIR" ]; then
            find "\$\$_DIR" -type f \( -name "*.log" -o -name "*.rej" \) -print >> "\$\$_LOG_DIR/file-list.txt" || true
          fi
        done
        if [ -s "\$\$_LOG_DIR/file-list.txt" ]; then
          tar -czf "\$\$_LOG_DIR/internal-logs.tar.gz" -T "\$\$_LOG_DIR/file-list.txt" || true
        fi
        ls -lh "\$\$_LOG_DIR" || true
        buildkite-agent artifact upload "\$\$_LOG_DIR/**/*" || true
      }
      # bazel-remote: write directly to ~/.bazelrc (Bazel reads unconditionally)
      # Use bash /dev/tcp (built-in, no nc required) to test connectivity
      BAZEL_REMOTE_URL="grpc://bazel-remote.build-tools.svc.cluster.local:9092"
      echo "=== Configuring bazel-remote cache ==="
      if timeout 5 bash -c "echo > /dev/tcp/bazel-remote.build-tools.svc.cluster.local/9092" 2>/dev/null; then
        echo "bazel-remote reachable: \$\${BAZEL_REMOTE_URL}"
        export REMOTE_CACHE="\$\${BAZEL_REMOTE_URL}"
      else
        echo "WARNING: bazel-remote not reachable, building without remote cache"
      fi
      
      echo "=== [2/3] Submodule Update ==="
      git submodule sync --recursive
      git submodule update --init --recursive --force --jobs=4
      
      echo "=== [3/6] Starting Build (JOBS=\$\$JOBS, FUNCTIONSYSTEM_JOBS=\$\$FUNCTIONSYSTEM_JOBS) ==="
      # Snapshot bazel-remote before build (to compute delta later)
      _BAZEL_PRE=\$\$(wget -qO- http://bazel-remote.build-tools.svc.cluster.local:8080/status 2>/dev/null || echo '{}')
      _BAZEL_FILES_PRE=\$\$(echo "\$\$_BAZEL_PRE" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('NumFiles',0))" 2>/dev/null || echo 0)
      _BAZEL_SIZE_PRE=\$\$(echo "\$\$_BAZEL_PRE" | python3 -c "import sys,json; d=json.load(sys.stdin); print(int(d.get('CurrSize',0))//1024//1024)" 2>/dev/null || echo 0)

      BUILD_START=\$\$(date +%s)
      # Capture build output for Bazel action stats while showing live output.
      # Named pipe approach: POSIX-compatible, captures make exit code correctly
      # (avoids PIPESTATUS which is bash-only and fails under /bin/sh).
      # Keep top-level target execution sequential for stability.
      rm -f /tmp/yr_build_pipe
      mkfifo /tmp/yr_build_pipe
      tee /tmp/build_output.log < /tmp/yr_build_pipe &
      set +e
      make all JOBS=\$\$JOBS FUNCTIONSYSTEM_JOBS=\$\$FUNCTIONSYSTEM_JOBS REMOTE_CACHE=\$\${REMOTE_CACHE:-} > /tmp/yr_build_pipe 2>&1
      BUILD_EXIT=\$\$?
      wait
      set -e
      rm -f /tmp/yr_build_pipe
      BUILD_END=\$\$(date +%s)
      BUILD_ELAPSED=\$\$(( BUILD_END - BUILD_START ))
      if [ "\$\$BUILD_EXIT" -ne 0 ]; then
        collect_failure_logs "\$\$BUILD_EXIT"
        exit "\$\$BUILD_EXIT"
      fi
      [ "\$\$BUILD_EXIT" -eq 0 ]

      echo "=== [3.1/6] Repackage release artifacts (version=\$\$YR_BUILD_VERSION) ==="
      export BUILD_VERSION="\$\$YR_BUILD_VERSION"
      rm -f output/openyuanrong-*.tar.gz output/openyuanrong-*.whl output/openyuanrong_sdk-*.whl
      bash scripts/package_yuanrong.sh -v "\$\$BUILD_VERSION"
      cd api/python
      rm -rf build/ dist/ *.egg-info
      SETUP_TYPE= PYTHON_RUNTIME_VERSION=python3.11 python3 setup.py bdist_wheel
      cp -R dist/*whl ../../output/
      cd ../..

      echo "=== [3.5/6] Verify Go plugin ABI ==="
      python3 .buildkite/verify_go_plugin_abi.py

      echo "=== [3.6/6] Package CI source snapshot ==="
      mkdir -p artifacts/ci-source
      git archive --format=tar HEAD | gzip > artifacts/ci-source/yuanrong-ci-source.tgz
      buildkite-agent artifact upload "artifacts/ci-source/yuanrong-ci-source.tgz"

      (
        set +e
        echo "=== [4/6] Cache Hit Report ==="
        # --- Bazel action stats from build output ---
        # Last "INFO: N processes: ..." line in the log has the final tally
        _BAZEL_PROC=\$\$(grep -E "^INFO: [0-9,]+ processes:" /tmp/build_output.log | tail -1 || echo "")
        if [ -n "\$\$_BAZEL_PROC" ]; then
          _BAZEL_TOTAL=\$\$(echo "\$\$_BAZEL_PROC" | grep -oE "[0-9,]+ processes" | tr -d ',')
          _BAZEL_TOTAL=\$\${_BAZEL_TOTAL% processes}
          _BAZEL_HIT=\$\$(echo "\$\$_BAZEL_PROC" | grep -oE "[0-9,]+ remote cache hit" | tr -d ',' | awk '{print \$\$1}' || echo 0)
          _BAZEL_LOCAL=\$\$(echo "\$\$_BAZEL_PROC" | grep -oE "[0-9,]+ processwrapper-sandbox" | tr -d ',' | awk '{print \$\$1}' || echo 0)
          _BAZEL_INTERNAL=\$\$(echo "\$\$_BAZEL_PROC" | grep -oE "[0-9,]+ internal" | tr -d ',' | awk '{print \$\$1}' || echo 0)
          if [ "\$\${_BAZEL_TOTAL:-0}" -gt 0 ] && [ "\$\${_BAZEL_HIT:-0}" -gt 0 ]; then
            _BAZEL_RATIO=\$\$(awk "BEGIN{printf \"%.1f\", \$\${_BAZEL_HIT}*100/(\$\${_BAZEL_TOTAL}-\$\${_BAZEL_INTERNAL:-0})}")
          else
            _BAZEL_RATIO="0.0"
          fi
          echo "bazel actions: total=\$\$_BAZEL_TOTAL internal=\$\$_BAZEL_INTERNAL cache_hit=\$\${_BAZEL_HIT:-0} local=\$\${_BAZEL_LOCAL:-0} hit_ratio=\$\$_BAZEL_RATIO%"
          echo "bazel summary: \$\$_BAZEL_PROC"
        else
          echo "bazel actions: (no summary found in log)"
          _BAZEL_HIT=0; _BAZEL_LOCAL=0; _BAZEL_RATIO="n/a"; _BAZEL_TOTAL=0; _BAZEL_INTERNAL=0
        fi

        # --- Bazel remote cache before/after delta ---
        _BAZEL_POST=\$\$(wget -qO- http://bazel-remote.build-tools.svc.cluster.local:8080/status 2>/dev/null || echo '{}')
        BAZEL_FILES=\$\$(echo "\$\$_BAZEL_POST" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('NumFiles',0))" 2>/dev/null || echo 0)
        BAZEL_SIZE_MB=\$\$(echo "\$\$_BAZEL_POST" | python3 -c "import sys,json; d=json.load(sys.stdin); print(int(d.get('CurrSize',0))//1024//1024)" 2>/dev/null || echo 0)
        _BAZEL_FILES_DELTA=\$\$(( \$\${BAZEL_FILES:-0} - \$\${_BAZEL_FILES_PRE:-0} ))
        _BAZEL_SIZE_DELTA=\$\$(( \$\${BAZEL_SIZE_MB:-0} - \$\${_BAZEL_SIZE_PRE:-0} ))
        echo "bazel-remote: files=\$\$BAZEL_FILES (+\$\$_BAZEL_FILES_DELTA), size=\$\${BAZEL_SIZE_MB}MB (+\$\${_BAZEL_SIZE_DELTA}MB)"

        # --- ccache ---
        CCACHE_HITS=\$\$(ccache -s 2>/dev/null | awk '/^cache hit/ {sum += \$\$NF} END {print sum+0}' || echo 0)
        CCACHE_MISS=\$\$(ccache -s 2>/dev/null | awk '/^cache miss/ {sum += \$\$NF} END {print sum+0}' || echo 0)
        CCACHE_TOTAL=\$\$(( CCACHE_HITS + CCACHE_MISS ))
        if [ "\$\$CCACHE_TOTAL" -gt 0 ]; then
          CCACHE_RATIO=\$\$(awk "BEGIN{printf \"%.1f\", \$\$CCACHE_HITS*100/\$\$CCACHE_TOTAL}")
        else
          CCACHE_RATIO="n/a"
        fi
        echo "ccache: hits=\$\$CCACHE_HITS miss=\$\$CCACHE_MISS ratio=\$\$CCACHE_RATIO%"

        # --- Go cache size ---
        GOCACHE_SIZE=\$\$(du -sh \$\$GOCACHE 2>/dev/null | cut -f1 || echo "?")
        GOMOD_SIZE=\$\$(du -sh \$\$GOMODCACHE 2>/dev/null | cut -f1 || echo "?")
        echo "go-cache: \$\${GOCACHE_SIZE}  go-mod: \$\${GOMOD_SIZE}"

        # --- datasystem CMake: fresh build each run, ccache handles incremental ---
        DS_CACHE_STATUS="fresh cmake (ccache backed)"
        echo "datasystem cmake: \$\$DS_CACHE_STATUS"

        # --- Build time ---
        BUILD_MIN=\$\$(( BUILD_ELAPSED / 60 ))
        BUILD_SEC=\$\$(( BUILD_ELAPSED % 60 ))
        echo "build time: \$\${BUILD_MIN}m\$\${BUILD_SEC}s"

        # --- Buildkite annotation (visible in UI) ---
        {
          echo "<h3>📊 Cache Hit Report</h3>"
          echo "<table>"
          echo "<tr><th>Cache</th><th>Detail</th><th>Hit Rate</th></tr>"
          echo "<tr><td>🔵 Bazel actions</td><td>total=\$\$_BAZEL_TOTAL, internal=\$\$_BAZEL_INTERNAL, hit=\$\${_BAZEL_HIT:-0}, local=\$\${_BAZEL_LOCAL:-0}</td><td><b>\$\$_BAZEL_RATIO%</b></td></tr>"
          echo "<tr><td>🗄️ Bazel remote store</td><td>\$\$BAZEL_FILES files (+\$\$_BAZEL_FILES_DELTA new), \$\${BAZEL_SIZE_MB}MB (+\$\${_BAZEL_SIZE_DELTA}MB)</td><td>-</td></tr>"
          echo "<tr><td>⚡ ccache</td><td>hits=\$\$CCACHE_HITS / total=\$\$CCACHE_TOTAL</td><td><b>\$\$CCACHE_RATIO%</b></td></tr>"
          echo "<tr><td>🐹 Go cache</td><td>\$\$GOCACHE_SIZE (module: \$\$GOMOD_SIZE)</td><td>-</td></tr>"
          echo "<tr><td>🏗️ datasystem cmake</td><td>\$\$DS_CACHE_STATUS</td><td>-</td></tr>"
          echo "<tr><td>⏱️ Build time</td><td colspan=2><b>\$\${BUILD_MIN}m\$\${BUILD_SEC}s</b></td></tr>"
          echo "</table>"
        } | buildkite-agent annotate --style "info" --context "cache-report"
      ) || echo "WARNING: cache report generation failed; continuing"

      echo "=== [5/6] Collect Artifacts ==="
      mkdir -p artifacts/release
      cp output/openyuanrong-*.tar.gz artifacts/release/ 2>/dev/null || true
      cp output/openyuanrong-*.whl artifacts/release/ 2>/dev/null || true
      ls -lh artifacts/release/
      bash .buildkite/upload_buildkite_packages.sh artifacts/release/*.whl

      echo "=== [6/6] Upload Artifacts to OBS ==="
      if [ -z "\$\${OBS_ACCESS_KEY_ID:-}" ] || [ -z "\$\${OBS_SECRET_ACCESS_KEY:-}" ]; then
        echo "WARNING: OBS credentials not set, skipping OBS upload"
      else
        python3 -c "from obs import ObsClient"
        UPLOAD_TIMESTAMP=\$\$(date '+%Y%m%d%H%M%S')
        UPLOAD_OK=0
        UPLOAD_FAIL=0
        OBS_URLS_FILE=artifacts/release/obs-urls.txt
        : > "\$\$OBS_URLS_FILE"
        for f in artifacts/release/openyuanrong-*.tar.gz artifacts/release/*.whl; do
          [ -f "\$\$f" ] || continue
          echo "Uploading: \$\$f"
          if UPLOAD_OUTPUT=\$\$(python3 tools/upload_build_artifact.py \
              --file "\$\$f" \
              --kind build \
              --channel daily \
              --arch "\$\${BUILD_ARCH:-x86_64}" \
              --platform linux \
              --timestamp "\$\$UPLOAD_TIMESTAMP" 2>&1); then
            printf '%s\n' "\$\$UPLOAD_OUTPUT"
            UPLOAD_OK=\$\$((UPLOAD_OK + 1))
            OBS_URL=\$\$(printf '%s\n' "\$\$UPLOAD_OUTPUT" | sed -n 's/^url: //p' | tail -n 1)
            if [ -n "\$\$OBS_URL" ]; then
              printf '%s\t%s\n' "\$\$(basename "\$\$f")" "\$\$OBS_URL" >> "\$\$OBS_URLS_FILE"
            else
              echo "WARNING: Uploaded \$\$f but did not find public OBS URL in uploader output"
            fi
          else
            printf '%s\n' "\$\$UPLOAD_OUTPUT"
            echo "WARNING: Failed to upload \$\$f"
            UPLOAD_FAIL=\$\$((UPLOAD_FAIL + 1))
          fi
        done
        if [ -s "\$\$OBS_URLS_FILE" ]; then
          buildkite-agent artifact upload "\$\$OBS_URLS_FILE"
          rm -f "\$\$OBS_URLS_FILE"
        else
          rm -f "\$\$OBS_URLS_FILE"
        fi
        echo "OBS upload complete: \$\$UPLOAD_OK succeeded, \$\$UPLOAD_FAIL failed"
        [ "\$\$UPLOAD_FAIL" -eq 0 ]
      fi

    agents:
      queue: "default"
      os: "linux"
      arch: "amd64"
    plugins:
      - kubernetes:
          podSpec:
            containers:
              - image: "$BUILDER_IMAGE"
                volumeMounts:
                  - name: build-cache
                    mountPath: /mnt/paas
                resources:
                  requests: { cpu: "16", memory: "32Gi" }
                  limits: { cpu: "16", memory: "32Gi" }
                env:
                  - name: GO111MODULE
                    value: "on"
                  - name: CGO_ENABLED
                    value: "1"
                  - name: GOPROXY
                    value: "https://goproxy.cn,direct"
                  - name: OBS_ACCESS_KEY_ID
                    valueFrom:
                      secretKeyRef:
                        name: obs-credentials
                        key: AK
                        optional: true
                  - name: OBS_SECRET_ACCESS_KEY
                    valueFrom:
                      secretKeyRef:
                        name: obs-credentials
                        key: SK
                        optional: true
                  - name: BUILDKITE_PACKAGE_UPLOAD_TOKEN
                    valueFrom:
                      secretKeyRef:
                        name: buildkite-package-credentials
                        key: api-token
                        optional: true
            volumes:
              - name: build-cache
                hostPath:
                  path: /mnt/paas
                  type: DirectoryOrCreate
    timeout_in_minutes: 180
    artifact_paths:
      - "artifacts/release/**/*"
YAML

for SDK_PYTHON_VERSION in ${SDK_PYTHON_VERSIONS}; do
  SDK_SUFFIX="$(sdk_python_suffix "${SDK_PYTHON_VERSION}")"
cat << YAML
  - label: ":snake: Build SDK X86 ${SDK_SUFFIX}"
    key: "build-sdk-amd64-${SDK_SUFFIX}"
    command: |
      set -e
      export PATH=/opt/buildtools/python3.9/bin:/usr/local/bin:/usr/bin:\$\$PATH
      export CC=gcc-10
      export CXX=g++-10
      export PIP_BREAK_SYSTEM_PACKAGES=1
      export SDK_PYTHON_VERSIONS="${SDK_PYTHON_VERSION}"
      COMMIT_FOR_VERSION="\$\${BUILDKITE_COMMIT:-\$\$(git rev-parse HEAD)}"
      SHORT_COMMIT=\$\$(printf '%s' "\$\$COMMIT_FOR_VERSION" | cut -c1-12)
      YR_BUILD_VERSION="\$\${YR_BUILD_VERSION:-\$\${BUILD_VERSION:-0.7.0+\$\$SHORT_COMMIT}}"
      export BUILD_VERSION="\$\$YR_BUILD_VERSION"
      export COMMIT_ID="\$\$COMMIT_FOR_VERSION"
      echo "Package version: \$\$BUILD_VERSION"

      CACHE_BASE="/mnt/paas/build-cache"
      mkdir -p \$\$CACHE_BASE/go-mod \$\$CACHE_BASE/go-cache \$\$CACHE_BASE/pip
      export GOMODCACHE=\$\$CACHE_BASE/go-mod
      export GOCACHE=\$\$CACHE_BASE/go-cache/amd64
      export PIP_CACHE_DIR=\$\$CACHE_BASE/pip
      BAZEL_REMOTE_URL="grpc://bazel-remote.build-tools.svc.cluster.local:9092"
      if timeout 5 bash -c "echo > /dev/tcp/bazel-remote.build-tools.svc.cluster.local/9092" 2>/dev/null; then
        export REMOTE_CACHE="\$\${BAZEL_REMOTE_URL}"
      fi

      git submodule sync --recursive
      git submodule update --init --recursive --force --jobs=4
      rm -f output/openyuanrong_sdk-*.whl
      bash .buildkite/build_openyuanrong_sdk_wheels.sh output

      rm -rf artifacts/openyuanrong-sdk
      mkdir -p artifacts/openyuanrong-sdk
      cp "output/openyuanrong_sdk-\$\$BUILD_VERSION"-*.whl artifacts/openyuanrong-sdk/
      ls -lh artifacts/openyuanrong-sdk/
      bash .buildkite/upload_buildkite_packages.sh artifacts/openyuanrong-sdk/*.whl

      echo "=== Upload openyuanrong-sdk to TestPyPI ==="
      if [ -z "\$\${TEST_PYPI_API_TOKEN:-}" ]; then
        echo "WARNING: TEST_PYPI_API_TOKEN not set, skipping TestPyPI upload"
      elif printf '%s\n' artifacts/openyuanrong-sdk/openyuanrong_sdk-*.whl | grep -q '+.*\.whl'; then
        echo "WARNING: openyuanrong-sdk uses local version \$\$BUILD_VERSION; PyPI/TestPyPI reject local versions, skipping TestPyPI upload"
      else
        python3 -m pip show twine >/dev/null 2>&1 || \
          python3 -m pip install -q --retries 2 --timeout 60 \
            --index-url "\$\${PIP_INDEX_URL:-https://mirrors.huaweicloud.com/repository/pypi/simple}" \
            --trusted-host "\$\${PIP_TRUSTED_HOST:-mirrors.huaweicloud.com}" \
            twine
        PYPI_ATTEMPT=1
        until TWINE_USERNAME=__token__ TWINE_PASSWORD="\$\$TEST_PYPI_API_TOKEN" \
          python3 -m twine upload \
            --repository-url https://test.pypi.org/legacy/ \
            --skip-existing \
            artifacts/openyuanrong-sdk/openyuanrong_sdk-*.whl; do
          if [ "\$\$PYPI_ATTEMPT" -ge 3 ]; then
            echo "ERROR: TestPyPI upload failed after \$\$PYPI_ATTEMPT attempts"
            exit 1
          fi
          PYPI_ATTEMPT=\$\$((PYPI_ATTEMPT + 1))
          echo "WARNING: TestPyPI upload failed; retrying attempt \$\$PYPI_ATTEMPT/3"
          sleep 15
        done
      fi
    agents:
      queue: "default"
      os: "linux"
      arch: "amd64"
    plugins:
      - kubernetes:
          podSpec:
            containers:
              - image: "$BUILDER_IMAGE"
                volumeMounts:
                  - name: build-cache
                    mountPath: /mnt/paas
                resources:
                  requests: { cpu: "4", memory: "8Gi" }
                  limits: { cpu: "8", memory: "16Gi" }
                env:
                  - name: GO111MODULE
                    value: "on"
                  - name: CGO_ENABLED
                    value: "1"
                  - name: GOPROXY
                    value: "https://goproxy.cn,direct"
                  - name: TEST_PYPI_API_TOKEN
                    valueFrom:
                      secretKeyRef:
                        name: test-pypi-credentials
                        key: api-token
                        optional: true
                  - name: BUILDKITE_PACKAGE_UPLOAD_TOKEN
                    valueFrom:
                      secretKeyRef:
                        name: buildkite-package-credentials
                        key: api-token
                        optional: true
            volumes:
              - name: build-cache
                hostPath:
                  path: /mnt/paas
                  type: DirectoryOrCreate
    timeout_in_minutes: 120
    artifact_paths:
      - "artifacts/openyuanrong-sdk/**/*"
YAML
done

case "${ENABLE_SANDBOX_PACKAGE}" in
  1|true|TRUE|yes|YES|on|ON)
cat << YAML
  - label: ":package: Build Image"
    key: "publish-sandbox-release-amd64"
    depends_on:
      - "build-all-amd64"
      - "${SANDBOX_AMD64_SDK_STEP}"
    command: |
      set -e
      buildkite-agent artifact download "artifacts/ci-source/yuanrong-ci-source.tgz" . --step "build-all-amd64"
      tar -xzf artifacts/ci-source/yuanrong-ci-source.tgz
      export PATH="/workspace:\$\$PATH"
      case "\$\${ENABLE_SANDBOX_MANIFEST:-}" in
        1|true|TRUE|yes|YES|on|ON)
          export YR_K8S_IMAGE_ARCH=amd64
          export YR_K8S_IMAGE_PLATFORM=linux/amd64
          export YR_K8S_IMAGE_CACHE_TAG=build-cache-amd64
          ;;
      esac
      bash .buildkite/package_sandbox_release.sh
    env:
      BUILDKITE_AGENT_NAME: "agent-stack-k8s"
      BUILDKITE_SKIP_CHECKOUT: "true"
      SANDBOX_SDK_STEP_KEY: "${SANDBOX_AMD64_SDK_STEP}"
      YR_K8S_IMAGE_CACHE: "1"
      ENABLE_SANDBOX_MANIFEST: "${ENABLE_SANDBOX_MANIFEST}"
    agents:
      queue: "default"
      os: "linux"
      arch: "amd64"
    plugins:
      - kubernetes:
          podSpec:
            imagePullSecrets:
              - name: swr-pull-secret
            containers:
              - image: "$SANDBOX_PACKAGER_IMAGE"
                securityContext:
                  privileged: true
                volumeMounts:
                  - name: docker-graph
                    mountPath: /var/lib/docker
                resources:
                  requests: { cpu: "8", memory: "16Gi" }
                  limits: { cpu: "8", memory: "16Gi" }
                env:
                  - name: OBS_ACCESS_KEY_ID
                    valueFrom:
                      secretKeyRef:
                        name: obs-credentials
                        key: AK
                        optional: true
                  - name: OBS_SECRET_ACCESS_KEY
                    valueFrom:
                      secretKeyRef:
                        name: obs-credentials
                        key: SK
                        optional: true
                  - name: SWR_USERNAME
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: username
                        optional: true
                  - name: SWR_PASSWORD
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: password
                        optional: true
                  - name: SWR_DOCKER_CONFIG_JSON
                    valueFrom:
                      secretKeyRef:
                        name: swr-pull-secret
                        key: .dockerconfigjson
                        optional: true
            volumes:
              - name: docker-graph
                emptyDir: {}
    timeout_in_minutes: 120
    artifact_paths:
      - "artifacts/sandbox/**/*"
YAML
for SDK_PYTHON_VERSION in ${SANDBOX_RUNTIME_IMAGE_PYTHON_VERSIONS}; do
  SDK_SUFFIX="$(sdk_python_suffix "${SDK_PYTHON_VERSION}")"
  emit_sandbox_runtime_image "amd64" "amd64" "${SDK_SUFFIX}" "build-sdk-amd64-${SDK_SUFFIX}" "$SANDBOX_PACKAGER_IMAGE" "1"
done
;;
esac

case "${ENABLE_LINUX_ARM}" in
  1|true|TRUE|yes|YES|on|ON)
cat << YAML
  - label: ":penguin: Build arm"
    key: "build-all-arm64"
    command: |
      set -e
      echo "=== [1/3] Environment Setup ==="

      export PATH=/opt/buildtools/python3.9/bin:/usr/local/bin:/usr/bin:\$\$PATH
      export CC=gcc-10
      export CXX=g++-10
      export PIP_BREAK_SYSTEM_PACKAGES=1
      export BUILD_ARCH=arm64
      export SDK_PYTHON_VERSIONS="${SDK_PYTHON_VERSIONS}"
      JOBS="\$\${JOBS:-16}"
      FUNCTIONSYSTEM_JOBS="\$\${FUNCTIONSYSTEM_JOBS:-8}"
      export FUNCTIONSYSTEM_JOBS
      COMMIT_FOR_VERSION="\$\${BUILDKITE_COMMIT:-\$\$(git rev-parse HEAD)}"
      SHORT_COMMIT=\$\$(printf '%s' "\$\$COMMIT_FOR_VERSION" | cut -c1-12)
      YR_BUILD_VERSION="\$\${YR_BUILD_VERSION:-\$\${BUILD_VERSION:-0.7.0+\$\$SHORT_COMMIT}}"
      export COMMIT_ID="\$\$COMMIT_FOR_VERSION"
      echo "Package version: \$\$YR_BUILD_VERSION"

      CACHE_BASE="/mnt/paas/build-cache"
      mkdir -p \$\$CACHE_BASE/ccache \$\$CACHE_BASE/go-mod \$\$CACHE_BASE/go-cache \
        \$\$CACHE_BASE/opensource \$\$CACHE_BASE/pip \$\$CACHE_BASE/functionsystem-vendor-cache
      export CCACHE_DIR=\$\$CACHE_BASE/ccache
      export CCACHE_MAXSIZE="\$\${CCACHE_MAXSIZE:-100G}"
      ccache -M "\$\$CCACHE_MAXSIZE"
      export GOMODCACHE=\$\$CACHE_BASE/go-mod
      export PIP_CACHE_DIR=\$\$CACHE_BASE/pip
      # Keep Go's content-addressed build cache warm across Buildkite jobs; cache-aging handles old entries.
      export GOCACHE=\$\$CACHE_BASE/go-cache/\$\${BUILD_ARCH:-arm64}
      mkdir -p "\$\$GOCACHE"
      export DS_OPENSOURCE_DIR=\$\$CACHE_BASE/opensource
      export FS_VENDOR_CACHE_DIR=\$\$CACHE_BASE/functionsystem-vendor-cache
      collect_failure_logs() {
        _STATUS="\$\$1"
        _ARCH="\$\${BUILD_ARCH:-x86_64}"
        _LOG_DIR="artifacts/failure-logs/\$\$_ARCH"
        echo "=== Build failed (exit \$\$_STATUS); collecting diagnostic logs ==="
        mkdir -p "\$\$_LOG_DIR"
        [ -f /tmp/build_output.log ] && cp /tmp/build_output.log "\$\$_LOG_DIR/build_output.log"
        for _DIR in \
            functionsystem/vendor/output/Stamp \
            functionsystem/vendor/src \
            functionsystem/build \
            datasystem/build; do
          if [ -d "\$\$_DIR" ]; then
            find "\$\$_DIR" -type f \( -name "*.log" -o -name "*.rej" \) -print >> "\$\$_LOG_DIR/file-list.txt" || true
          fi
        done
        if [ -s "\$\$_LOG_DIR/file-list.txt" ]; then
          tar -czf "\$\$_LOG_DIR/internal-logs.tar.gz" -T "\$\$_LOG_DIR/file-list.txt" || true
        fi
        ls -lh "\$\$_LOG_DIR" || true
        buildkite-agent artifact upload "\$\$_LOG_DIR/**/*" || true
      }
      BAZEL_REMOTE_URL="grpc://bazel-remote.build-tools.svc.cluster.local:9092"
      echo "=== Configuring bazel-remote cache ==="
      if timeout 5 bash -c "echo > /dev/tcp/bazel-remote.build-tools.svc.cluster.local/9092" 2>/dev/null; then
        echo "bazel-remote reachable: \$\${BAZEL_REMOTE_URL}"
        export REMOTE_CACHE="\$\${BAZEL_REMOTE_URL}"
      else
        echo "WARNING: bazel-remote not reachable, building without remote cache"
      fi

      echo "=== [2/3] Submodule Update ==="
      git submodule sync --recursive
      git submodule update --init --recursive --force --jobs=4

      echo "=== [3/6] Starting Build (JOBS=\$\$JOBS, FUNCTIONSYSTEM_JOBS=\$\$FUNCTIONSYSTEM_JOBS) ==="
      _BAZEL_PRE=\$\$(wget -qO- http://bazel-remote.build-tools.svc.cluster.local:8080/status 2>/dev/null || echo '{}')
      _BAZEL_FILES_PRE=\$\$(echo "\$\$_BAZEL_PRE" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('NumFiles',0))" 2>/dev/null || echo 0)
      _BAZEL_SIZE_PRE=\$\$(echo "\$\$_BAZEL_PRE" | python3 -c "import sys,json; d=json.load(sys.stdin); print(int(d.get('CurrSize',0))//1024//1024)" 2>/dev/null || echo 0)

      BUILD_START=\$\$(date +%s)
      rm -f /tmp/yr_build_pipe
      mkfifo /tmp/yr_build_pipe
      tee /tmp/build_output.log < /tmp/yr_build_pipe &
      set +e
      make all JOBS=\$\$JOBS FUNCTIONSYSTEM_JOBS=\$\$FUNCTIONSYSTEM_JOBS REMOTE_CACHE=\$\${REMOTE_CACHE:-} > /tmp/yr_build_pipe 2>&1
      BUILD_EXIT=\$\$?
      wait
      set -e
      rm -f /tmp/yr_build_pipe
      BUILD_END=\$\$(date +%s)
      BUILD_ELAPSED=\$\$(( BUILD_END - BUILD_START ))
      if [ "\$\$BUILD_EXIT" -ne 0 ]; then
        collect_failure_logs "\$\$BUILD_EXIT"
        exit "\$\$BUILD_EXIT"
      fi
      [ "\$\$BUILD_EXIT" -eq 0 ]

      echo "=== [3.1/6] Repackage release artifacts (version=\$\$YR_BUILD_VERSION) ==="
      export BUILD_VERSION="\$\$YR_BUILD_VERSION"
      rm -f output/openyuanrong-*.tar.gz output/openyuanrong-*.whl output/openyuanrong_sdk-*.whl
      bash scripts/package_yuanrong.sh -v "\$\$BUILD_VERSION"
      cd api/python
      rm -rf build/ dist/ *.egg-info
      SETUP_TYPE= PYTHON_RUNTIME_VERSION=python3.11 python3 setup.py bdist_wheel
      cp -R dist/*whl ../../output/
      cd ../..

      echo "=== [3.5/6] Verify Go plugin ABI ==="
      python3 .buildkite/verify_go_plugin_abi.py

      (
        set +e
        echo "=== [4/6] Cache Hit Report ==="
        _BAZEL_PROC=\$\$(grep -E "^INFO: [0-9,]+ processes:" /tmp/build_output.log | tail -1 || echo "")
        if [ -n "\$\$_BAZEL_PROC" ]; then
          _BAZEL_TOTAL=\$\$(echo "\$\$_BAZEL_PROC" | grep -oE "[0-9,]+ processes" | tr -d ',')
          _BAZEL_TOTAL=\$\${_BAZEL_TOTAL% processes}
          _BAZEL_HIT=\$\$(echo "\$\$_BAZEL_PROC" | grep -oE "[0-9,]+ remote cache hit" | tr -d ',' | awk '{print \$\$1}' || echo 0)
          _BAZEL_LOCAL=\$\$(echo "\$\$_BAZEL_PROC" | grep -oE "[0-9,]+ processwrapper-sandbox" | tr -d ',' | awk '{print \$\$1}' || echo 0)
          _BAZEL_INTERNAL=\$\$(echo "\$\$_BAZEL_PROC" | grep -oE "[0-9,]+ internal" | tr -d ',' | awk '{print \$\$1}' || echo 0)
          if [ "\$\${_BAZEL_TOTAL:-0}" -gt 0 ] && [ "\$\${_BAZEL_HIT:-0}" -gt 0 ]; then
            _BAZEL_RATIO=\$\$(awk "BEGIN{printf \"%.1f\", \$\${_BAZEL_HIT}*100/(\$\${_BAZEL_TOTAL}-\$\${_BAZEL_INTERNAL:-0})}")
          else
            _BAZEL_RATIO="0.0"
          fi
          echo "bazel actions: total=\$\$_BAZEL_TOTAL internal=\$\$_BAZEL_INTERNAL cache_hit=\$\${_BAZEL_HIT:-0} local=\$\${_BAZEL_LOCAL:-0} hit_ratio=\$\$_BAZEL_RATIO%"
          echo "bazel summary: \$\$_BAZEL_PROC"
        else
          echo "bazel actions: (no summary found in log)"
          _BAZEL_HIT=0; _BAZEL_LOCAL=0; _BAZEL_RATIO="n/a"; _BAZEL_TOTAL=0; _BAZEL_INTERNAL=0
        fi

        _BAZEL_POST=\$\$(wget -qO- http://bazel-remote.build-tools.svc.cluster.local:8080/status 2>/dev/null || echo '{}')
        BAZEL_FILES=\$\$(echo "\$\$_BAZEL_POST" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('NumFiles',0))" 2>/dev/null || echo 0)
        BAZEL_SIZE_MB=\$\$(echo "\$\$_BAZEL_POST" | python3 -c "import sys,json; d=json.load(sys.stdin); print(int(d.get('CurrSize',0))//1024//1024)" 2>/dev/null || echo 0)
        _BAZEL_FILES_DELTA=\$\$(( \$\${BAZEL_FILES:-0} - \$\${_BAZEL_FILES_PRE:-0} ))
        _BAZEL_SIZE_DELTA=\$\$(( \$\${BAZEL_SIZE_MB:-0} - \$\${_BAZEL_SIZE_PRE:-0} ))
        echo "bazel-remote: files=\$\$BAZEL_FILES (+\$\$_BAZEL_FILES_DELTA), size=\$\${BAZEL_SIZE_MB}MB (+\$\${_BAZEL_SIZE_DELTA}MB)"

        CCACHE_HITS=\$\$(ccache -s 2>/dev/null | awk '/^cache hit/ {sum += \$\$NF} END {print sum+0}' || echo 0)
        CCACHE_MISS=\$\$(ccache -s 2>/dev/null | awk '/^cache miss/ {sum += \$\$NF} END {print sum+0}' || echo 0)
        CCACHE_TOTAL=\$\$(( CCACHE_HITS + CCACHE_MISS ))
        if [ "\$\$CCACHE_TOTAL" -gt 0 ]; then
          CCACHE_RATIO=\$\$(awk "BEGIN{printf \"%.1f\", \$\$CCACHE_HITS*100/\$\$CCACHE_TOTAL}")
        else
          CCACHE_RATIO="n/a"
        fi
        echo "ccache: hits=\$\$CCACHE_HITS miss=\$\$CCACHE_MISS ratio=\$\$CCACHE_RATIO%"

        GOCACHE_SIZE=\$\$(du -sh \$\$GOCACHE 2>/dev/null | cut -f1 || echo "?")
        GOMOD_SIZE=\$\$(du -sh \$\$GOMODCACHE 2>/dev/null | cut -f1 || echo "?")
        echo "go-cache: \$\${GOCACHE_SIZE}  go-mod: \$\${GOMOD_SIZE}"

        DS_CACHE_STATUS="fresh cmake (ccache backed)"
        echo "datasystem cmake: \$\$DS_CACHE_STATUS"

        BUILD_MIN=\$\$(( BUILD_ELAPSED / 60 ))
        BUILD_SEC=\$\$(( BUILD_ELAPSED % 60 ))
        echo "build time: \$\${BUILD_MIN}m\$\${BUILD_SEC}s"

        {
          echo "<h3>📊 Cache Hit Report</h3>"
          echo "<table>"
          echo "<tr><th>Cache</th><th>Detail</th><th>Hit Rate</th></tr>"
          echo "<tr><td>🔵 Bazel actions</td><td>total=\$\$_BAZEL_TOTAL, internal=\$\$_BAZEL_INTERNAL, hit=\$\${_BAZEL_HIT:-0}, local=\$\${_BAZEL_LOCAL:-0}</td><td><b>\$\$_BAZEL_RATIO%</b></td></tr>"
          echo "<tr><td>🗄️ Bazel remote store</td><td>\$\$BAZEL_FILES files (+\$\$_BAZEL_FILES_DELTA new), \$\${BAZEL_SIZE_MB}MB (+\$\${_BAZEL_SIZE_DELTA}MB)</td><td>-</td></tr>"
          echo "<tr><td>⚡ ccache</td><td>hits=\$\$CCACHE_HITS / total=\$\$CCACHE_TOTAL</td><td><b>\$\$CCACHE_RATIO%</b></td></tr>"
          echo "<tr><td>🐹 Go cache</td><td>\$\$GOCACHE_SIZE (module: \$\$GOMOD_SIZE)</td><td>-</td></tr>"
          echo "<tr><td>🏗️ datasystem cmake</td><td>\$\$DS_CACHE_STATUS</td><td>-</td></tr>"
          echo "<tr><td>⏱️ Build time</td><td colspan=2><b>\$\${BUILD_MIN}m\$\${BUILD_SEC}s</b></td></tr>"
          echo "</table>"
        } | buildkite-agent annotate --style "info" --context "cache-report-arm64"
      ) || echo "WARNING: cache report generation failed; continuing"

      echo "=== [5/6] Collect Artifacts ==="
      mkdir -p artifacts/release
      cp output/openyuanrong-*.tar.gz artifacts/release/ 2>/dev/null || true
      cp output/openyuanrong-*.whl artifacts/release/ 2>/dev/null || true
      ls -lh artifacts/release/
      bash .buildkite/upload_buildkite_packages.sh artifacts/release/*.whl

      echo "=== [6/6] Upload Artifacts to OBS ==="
      if [ -z "\$\${OBS_ACCESS_KEY_ID:-}" ] || [ -z "\$\${OBS_SECRET_ACCESS_KEY:-}" ]; then
        echo "WARNING: OBS credentials not set, skipping OBS upload"
      else
        python3 -c "from obs import ObsClient"
        UPLOAD_TIMESTAMP=\$\$(date '+%Y%m%d%H%M%S')
        UPLOAD_OK=0
        UPLOAD_FAIL=0
        OBS_URLS_FILE=artifacts/release/obs-urls.txt
        : > "\$\$OBS_URLS_FILE"
        for f in artifacts/release/openyuanrong-*.tar.gz artifacts/release/*.whl; do
          [ -f "\$\$f" ] || continue
          echo "Uploading: \$\$f"
          if UPLOAD_OUTPUT=\$\$(python3 tools/upload_build_artifact.py \
              --file "\$\$f" \
              --kind build \
              --channel daily \
              --arch arm64 \
              --platform linux \
              --timestamp "\$\$UPLOAD_TIMESTAMP" 2>&1); then
            printf '%s\n' "\$\$UPLOAD_OUTPUT"
            UPLOAD_OK=\$\$((UPLOAD_OK + 1))
            OBS_URL=\$\$(printf '%s\n' "\$\$UPLOAD_OUTPUT" | sed -n 's/^url: //p' | tail -n 1)
            if [ -n "\$\$OBS_URL" ]; then
              printf '%s\t%s\n' "\$\$(basename "\$\$f")" "\$\$OBS_URL" >> "\$\$OBS_URLS_FILE"
            else
              echo "WARNING: Uploaded \$\$f but did not find public OBS URL in uploader output"
            fi
          else
            printf '%s\n' "\$\$UPLOAD_OUTPUT"
            echo "WARNING: Failed to upload \$\$f"
            UPLOAD_FAIL=\$\$((UPLOAD_FAIL + 1))
          fi
        done
        if [ -s "\$\$OBS_URLS_FILE" ]; then
          buildkite-agent artifact upload "\$\$OBS_URLS_FILE"
          rm -f "\$\$OBS_URLS_FILE"
        else
          rm -f "\$\$OBS_URLS_FILE"
        fi
        echo "OBS upload complete: \$\$UPLOAD_OK succeeded, \$\$UPLOAD_FAIL failed"
        [ "\$\$UPLOAD_FAIL" -eq 0 ]
      fi
    agents:
      queue: "default"
      os: "linux"
      linux_arch: "arm64"
    plugins:
      - kubernetes:
          podSpec:
            nodeSelector:
              kubernetes.io/arch: arm64
            containers:
              - image: "$BUILDER_IMAGE"
                volumeMounts:
                  - name: build-cache
                    mountPath: /mnt/paas
                resources:
                  requests: { cpu: "16", memory: "32Gi" }
                  limits: { cpu: "16", memory: "32Gi" }
                env:
                  - name: GO111MODULE
                    value: "on"
                  - name: CGO_ENABLED
                    value: "1"
                  - name: GOARCH
                    value: "arm64"
                  - name: GOPROXY
                    value: "https://goproxy.cn,direct"
                  - name: OBS_ACCESS_KEY_ID
                    valueFrom:
                      secretKeyRef:
                        name: obs-credentials
                        key: AK
                        optional: true
                  - name: OBS_SECRET_ACCESS_KEY
                    valueFrom:
                      secretKeyRef:
                        name: obs-credentials
                        key: SK
                        optional: true
                  - name: BUILDKITE_PACKAGE_UPLOAD_TOKEN
                    valueFrom:
                      secretKeyRef:
                        name: buildkite-package-credentials
                        key: api-token
                        optional: true
            volumes:
              - name: build-cache
                hostPath:
                  path: /mnt/paas
                  type: DirectoryOrCreate
    timeout_in_minutes: 180
    artifact_paths:
      - "artifacts/release/**/*"
YAML
for SDK_PYTHON_VERSION in ${SDK_PYTHON_VERSIONS}; do
  SDK_SUFFIX="$(sdk_python_suffix "${SDK_PYTHON_VERSION}")"
cat << YAML
  - label: ":snake: Build SDK arm ${SDK_SUFFIX}"
    key: "build-sdk-arm64-${SDK_SUFFIX}"
    command: |
      set -e
      export PATH=/opt/buildtools/python3.9/bin:/usr/local/bin:/usr/bin:\$\$PATH
      export CC=gcc-10
      export CXX=g++-10
      export PIP_BREAK_SYSTEM_PACKAGES=1
      export BUILD_ARCH=arm64
      export SDK_PYTHON_VERSIONS="${SDK_PYTHON_VERSION}"
      COMMIT_FOR_VERSION="\$\${BUILDKITE_COMMIT:-\$\$(git rev-parse HEAD)}"
      SHORT_COMMIT=\$\$(printf '%s' "\$\$COMMIT_FOR_VERSION" | cut -c1-12)
      YR_BUILD_VERSION="\$\${YR_BUILD_VERSION:-\$\${BUILD_VERSION:-0.7.0+\$\$SHORT_COMMIT}}"
      export BUILD_VERSION="\$\$YR_BUILD_VERSION"
      export COMMIT_ID="\$\$COMMIT_FOR_VERSION"
      echo "Package version: \$\$BUILD_VERSION"

      CACHE_BASE="/mnt/paas/build-cache"
      mkdir -p \$\$CACHE_BASE/go-mod \$\$CACHE_BASE/go-cache \$\$CACHE_BASE/pip
      export GOMODCACHE=\$\$CACHE_BASE/go-mod
      export GOCACHE=\$\$CACHE_BASE/go-cache/arm64
      export PIP_CACHE_DIR=\$\$CACHE_BASE/pip
      BAZEL_REMOTE_URL="grpc://bazel-remote.build-tools.svc.cluster.local:9092"
      if timeout 5 bash -c "echo > /dev/tcp/bazel-remote.build-tools.svc.cluster.local/9092" 2>/dev/null; then
        export REMOTE_CACHE="\$\${BAZEL_REMOTE_URL}"
      fi

      git submodule sync --recursive
      git submodule update --init --recursive --force --jobs=4
      rm -f output/openyuanrong_sdk-*.whl
      bash .buildkite/build_openyuanrong_sdk_wheels.sh output

      rm -rf artifacts/openyuanrong-sdk
      mkdir -p artifacts/openyuanrong-sdk
      cp "output/openyuanrong_sdk-\$\$BUILD_VERSION"-*.whl artifacts/openyuanrong-sdk/
      ls -lh artifacts/openyuanrong-sdk/
      bash .buildkite/upload_buildkite_packages.sh artifacts/openyuanrong-sdk/*.whl
    agents:
      queue: "default"
      os: "linux"
      linux_arch: "arm64"
    plugins:
      - kubernetes:
          podSpec:
            nodeSelector:
              kubernetes.io/arch: arm64
            containers:
              - image: "$BUILDER_IMAGE"
                volumeMounts:
                  - name: build-cache
                    mountPath: /mnt/paas
                resources:
                  requests: { cpu: "4", memory: "8Gi" }
                  limits: { cpu: "8", memory: "16Gi" }
                env:
                  - name: GO111MODULE
                    value: "on"
                  - name: CGO_ENABLED
                    value: "1"
                  - name: GOARCH
                    value: "arm64"
                  - name: GOPROXY
                    value: "https://goproxy.cn,direct"
                  - name: BUILDKITE_PACKAGE_UPLOAD_TOKEN
                    valueFrom:
                      secretKeyRef:
                        name: buildkite-package-credentials
                        key: api-token
                        optional: true
            volumes:
              - name: build-cache
                hostPath:
                  path: /mnt/paas
                  type: DirectoryOrCreate
    timeout_in_minutes: 120
    artifact_paths:
      - "artifacts/openyuanrong-sdk/**/*"
YAML
done
if is_enabled "${ENABLE_SANDBOX_PACKAGE}" && is_enabled "${ENABLE_SANDBOX_MANIFEST}"; then
cat << YAML
  - label: ":package: Build Image arm"
    key: "publish-sandbox-release-arm64"
    depends_on:
      - "build-all-arm64"
      - "${SANDBOX_ARM64_SDK_STEP}"
    command: |
      set -e
      buildkite-agent artifact download "artifacts/ci-source/yuanrong-ci-source.tgz" . --step "build-all-amd64"
      tar -xzf artifacts/ci-source/yuanrong-ci-source.tgz
      export PATH="/workspace:\$\$PATH"
      bash .buildkite/package_sandbox_release.sh
    env:
      BUILDKITE_AGENT_NAME: "agent-stack-k8s"
      BUILDKITE_SKIP_CHECKOUT: "true"
      SANDBOX_BUILD_STEP_KEY: "build-all-arm64"
      SANDBOX_SDK_STEP_KEY: "${SANDBOX_ARM64_SDK_STEP}"
      YR_K8S_IMAGE_ARCH: "arm64"
      YR_K8S_IMAGE_PLATFORM: "linux/arm64"
      YR_K8S_IMAGE_CACHE: "1"
      YR_K8S_IMAGE_CACHE_TAG: "build-cache-arm64"
      YR_K8S_DOCKER_BUILDKIT: "0"
    agents:
      queue: "default"
      os: "linux"
      linux_arch: "arm64"
    plugins:
      - kubernetes:
          podSpec:
            nodeSelector:
              kubernetes.io/arch: arm64
            imagePullSecrets:
              - name: swr-pull-secret
            containers:
              - image: "$BUILDER_IMAGE"
                securityContext:
                  privileged: true
                volumeMounts:
                  - name: docker-graph
                    mountPath: /var/lib/docker
                resources:
                  requests: { cpu: "8", memory: "16Gi" }
                  limits: { cpu: "8", memory: "16Gi" }
                env:
                  - name: OBS_ACCESS_KEY_ID
                    valueFrom:
                      secretKeyRef:
                        name: obs-credentials
                        key: AK
                        optional: true
                  - name: OBS_SECRET_ACCESS_KEY
                    valueFrom:
                      secretKeyRef:
                        name: obs-credentials
                        key: SK
                        optional: true
                  - name: SWR_USERNAME
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: username
                        optional: true
                  - name: SWR_PASSWORD
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: password
                        optional: true
                  - name: SWR_DOCKER_CONFIG_JSON
                    valueFrom:
                      secretKeyRef:
                        name: swr-pull-secret
                        key: .dockerconfigjson
                        optional: true
            volumes:
              - name: docker-graph
                emptyDir: {}
    timeout_in_minutes: 120
    artifact_paths:
      - "artifacts/sandbox/**/*"
YAML
for SDK_PYTHON_VERSION in ${SANDBOX_RUNTIME_IMAGE_PYTHON_VERSIONS}; do
  SDK_SUFFIX="$(sdk_python_suffix "${SDK_PYTHON_VERSION}")"
  emit_sandbox_runtime_image "arm64" "arm64" "${SDK_SUFFIX}" "build-sdk-arm64-${SDK_SUFFIX}" "$BUILDER_IMAGE" "0"
done
cat << YAML

  - label: ":package: Build Manifest"
    key: "publish-sandbox-manifest"
    depends_on:
      - "publish-sandbox-release-amd64"
      - "publish-sandbox-release-arm64"
${SANDBOX_MANIFEST_SDK_DEPENDS}
${SANDBOX_MANIFEST_RUNTIME_DEPENDS}
    command: |
      set -e
      buildkite-agent artifact download "artifacts/ci-source/yuanrong-ci-source.tgz" . --step "build-all-amd64"
      tar -xzf artifacts/ci-source/yuanrong-ci-source.tgz
      export PATH="/workspace:\$\$PATH"
      bash .buildkite/package_sandbox_manifest.sh
    env:
      BUILDKITE_AGENT_NAME: "agent-stack-k8s"
      BUILDKITE_SKIP_CHECKOUT: "true"
      SANDBOX_RUNTIME_IMAGE_STEPS: "${SANDBOX_RUNTIME_IMAGE_STEPS}"
    agents:
      queue: "default"
      os: "linux"
      arch: "amd64"
    plugins:
      - kubernetes:
          podSpec:
            imagePullSecrets:
              - name: swr-pull-secret
            containers:
              - image: "$SANDBOX_PACKAGER_IMAGE"
                resources:
                  requests: { cpu: "1", memory: "1Gi" }
                  limits: { cpu: "2", memory: "2Gi" }
                env:
                  - name: SWR_USERNAME
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: username
                        optional: true
                  - name: SWR_PASSWORD
                    valueFrom:
                      secretKeyRef:
                        name: swr-credentials
                        key: password
                        optional: true
                  - name: SWR_DOCKER_CONFIG_JSON
                    valueFrom:
                      secretKeyRef:
                        name: swr-pull-secret
                        key: .dockerconfigjson
                        optional: true
    timeout_in_minutes: 30
    artifact_paths:
      - "artifacts/sandbox/**/*"
YAML
fi
;;
esac

if is_enabled "${ENABLE_SANDBOX_PACKAGE}" && is_enabled "${ENABLE_SANDBOX_K8S_TEST}"; then
  emit_sandbox_k8s_test
fi

case "${ENABLE_MACOS_SDK}" in
  1|true|TRUE|yes|YES|on|ON)
for SDK_PYTHON_VERSION in ${SDK_PYTHON_VERSIONS}; do
  SDK_SUFFIX="$(sdk_python_suffix "${SDK_PYTHON_VERSION}")"
cat << YAML
  - label: ":snake: Build SDK macOS ${SDK_SUFFIX}"
    key: "build-sdk-macos-arm64-${SDK_SUFFIX}"
    command: |
      set -e
      echo "=== [1/4] Sync submodules ==="
      git submodule sync --recursive
      git submodule update --init --recursive --force --jobs=4
      COMMIT_FOR_VERSION="\$\${BUILDKITE_COMMIT:-\$\$(git rev-parse HEAD)}"
      SHORT_COMMIT=\$\$(printf '%s' "\$\$COMMIT_FOR_VERSION" | cut -c1-12)
      YR_BUILD_VERSION="\$\${YR_BUILD_VERSION:-\$\${BUILD_VERSION:-0.7.0+\$\$SHORT_COMMIT}}"
      export BUILD_VERSION="\$\$YR_BUILD_VERSION"
      export COMMIT_ID="\$\$COMMIT_FOR_VERSION"
      export SDK_PYTHON_VERSIONS="${SDK_PYTHON_VERSION}"
      unset REMOTE_CACHE
      echo "Package version: \$\$BUILD_VERSION"

      echo "=== [2/4] Ensure macOS SDK toolchain ==="
      SKIP_BREW_UPDATE="\$\${SKIP_BREW_UPDATE:-1}" bash scripts/ensure-macos-build-tools.sh

      echo "=== [3/4] Build openyuanrong-sdk wheels ==="
      rm -f output/openyuanrong_sdk-*.whl
      bash .buildkite/build_openyuanrong_sdk_wheels.sh output

      echo "=== [4/4] Collect openyuanrong-sdk artifacts ==="
      rm -rf artifacts/openyuanrong-sdk
      mkdir -p artifacts/openyuanrong-sdk
      cp "output/openyuanrong_sdk-\$\$BUILD_VERSION"-*.whl artifacts/openyuanrong-sdk/
      ls -lh artifacts/openyuanrong-sdk/
      bash .buildkite/upload_buildkite_packages.sh artifacts/openyuanrong-sdk/*.whl
    agents:
      queue: "default"
      os: "macos"
      arch: "arm64"
    timeout_in_minutes: 120
    artifact_paths:
      - "artifacts/openyuanrong-sdk/**/*"
YAML
done
;;
esac
